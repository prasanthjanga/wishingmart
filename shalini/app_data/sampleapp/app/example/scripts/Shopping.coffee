'use strict'

angular.module('wishingMartApp')
  .controller 'ShoppingCtrl', ($scope, $timeout) ->
    # Bind controller data
    do ->

    # Attach listeners


    # Initialize controller
    do ->
      steroids.view.navigationBar.show ' '
